# RightMenuX UI 设计系统 v3.0（现代简约卡片风）

> UI Designer：像素君 ｜ 日期：2026-07-22 ｜ 状态：待评审
> 平台：Windows 桌面（Win32 GDI 自绘）｜ 目标：全面重设计

---

## 🎨 Design Foundations

### 色彩系统（Color Tokens）

| Token | Hex | 用途 |
|---|---|---|
| `--bg` | `#F7F8FA` | 窗口背景（极浅灰，区别于卡片白） |
| `--surface` | `#FFFFFF` | 卡片 / 控件表面 |
| `--border` | `#E8EAED` | 卡片边框、分隔线 |
| `--text-1` | `#1F2430` | 主标题、功能名（近黑） |
| `--text-2` | `#6B7280` | 副标题、说明文字（中灰） |
| `--text-3` | `#9CA3AF` | 页脚提示、辅助文字（浅灰） |
| `--primary` | `#2563EB` | 主操作按钮、品牌强调 |
| `--primary-hover` | `#1D4ED8` | 主按钮悬停 |
| `--green` | `#16A34A` | RunCommand 类型标识 / 已启用 |
| `--green-bg` | `#E7F6EC` | 已启用徽章底色 |
| `--amber` | `#D97706` | ShellExtension 类型标识 |
| `--amber-bg` | `#FDF1E0` | （预留）未启用暖色徽章 |

**对比度校验（WCAG AA）**
- `--text-1` on `--surface`：#1F2430 / #FFFFFF → 对比度 ≈ 15.8:1 ✅（远超 4.5:1）
- `--text-2` on `--surface`：#6B7280 / #FFFFFF → 对比度 ≈ 4.6:1 ✅（达标）
- 白字 on `--primary`：#FFFFFF / #2563EB → 对比度 ≈ 4.5:1 ✅（临界达标，正文不依赖此组合）

### 字体系统（Typography）

| 角色 | 字号 | 字重 | 行高 | 说明 |
|---|---|---|---|---|
| 窗口标题 | 24px | 700 | 32px | "Windows 个性化" |
| 功能名 | 16px | 600 | 22px | 卡片主标题（v1.0.0.19 起由 15px 放大） |
| 说明文字 | 12px | 400 | 18px | 卡片副描述 |
| 状态徽章 | 11px | 500 | 16px | 药丸内文字 |
| 按钮文字 | 13px | 500 | — | 启用/停用 |
| OS 副标题 | 13px | 400 | 18px | Windows 11 (Build xxxx) |
| 页脚提示 | 11px | 400 | 16px | 使用说明 |

**字体栈**：`"Microsoft YaHei", "Segoe UI", system-ui, sans-serif`（中文优先雅黑，英文回退 Segoe）

### 间距系统（Spacing · 4px 基准）

```
4 / 8 / 12 / 16 / 24 / 32 px
```
约定：
- 卡片内边距 `16px 18px`
- 卡片之间间距 `14px`
- 标题区下边距 `20px`
- 图标与文字间距 `16px`
- 文字行组间距：标题→说明 `2px`、说明→徽章 `8px`

### 阴影与层级（Elevation）

```
--shadow-sm: 0 1px 2px rgba(0,0,0,0.04), 0 1px 3px rgba(0,0,0,0.06);
--shadow-md: 0 4px 12px rgba(0,0,0,0.08);   /* hover 态预留 */
```
卡片默认 `--shadow-sm`；左缘 3px 彩色标识条区分功能类型（绿=RunCommand / 琥珀=ShellExtension）。

---

## 🧱 Component Library

### 1. 窗口框架（Window）
- 宽度：560px（紧凑不浪费）
- 背景：`--bg`
- 标题栏：系统原生（WS_CAPTION），不做自绘标题栏以保系统习惯

### 2. 页头（Header）
- 24px 粗体标题 + 32px×3px 品牌蓝装饰短线 + 13px 灰色 OS 副标题
- 下方 1px 分隔线

### 3. 功能卡片（Feature Card）
```
┌─(3px 彩条)──────────────────────────────────────┐
│  ┌────┐                                          │
│  │图标│  功能名称                     [启用/停用] │
│  │44px│  说明文字...                              │
│  └────┘  ● 已启用 / ○ 未启用                     │
└──────────────────────────────────────────────────┘
```
- 白色圆角 12px，1px 边框，微阴影
- 左侧 3px 类型彩条（圆角右端）
- 图标：44px 圆角方块（绿/琥珀）+ 白色滑块纹
- 文字区：标题→说明→状态徽章，垂直堆叠不重叠
- 按钮：右侧垂直居中

### 4. 状态徽章（Status Badge）
- 药丸形（border-radius 11px）
- 已启用：绿底 `#E7F6EC` + 绿字 `#16A34A`
- 未启用：灰底 `#F3F4F6` + 灰字 `#9CA3AF`
- 固定宽 70px、高 22px

### 5. 按钮（Button）
| 类型 | 样式 | 悬停 |
|---|---|---|
| 主操作（启用） | 蓝底白字，圆角 5px | 深蓝 `#1D4ED8` |
| 次操作（停用） | 白底深字 + 浅灰边 | 浅灰底 `#F0F2F5` |
- 尺寸：90×32px，足够点击区域（>44px 高度要求仅触屏必需，桌面 32px OK）

### 6. 页脚（Footer）
- 11px 浅灰提示文字，说明右键菜单位置与管理员权限需求

---

## 📐 布局规格（Layout Spec）

| 区域 | 高度 | 说明 |
|---|---|---|
| 页头 | 120px | 标题(24) + 装饰线 + OS副标题 |
| 卡片 × N | 104px 每个 | 含内边距与内容 |
| 卡片间距 | 14px | — |
| 页脚 | 56px | 提示文字 |

**卡片内部垂直坐标（CARD_H=104，top=0）：**
```
y+12 ~ y+36   功能名（24px）
y+38 ~ y+58   说明（20px）   ← 与标题间隔 2px
y+66 ~ y+88   状态徽章（22px）← 与说明间隔 8px
底部留白 16px
```
**零重叠验证**：36 < 38，58 < 66 ✅

---

## ♿ 无障碍（Accessibility）

- 文字对比度全部 ≥ 4.5:1（见上）
- 按钮/徽章均有清晰边界与状态色区分
- 悬停态有明确色彩反馈
- 键盘：ESC 关闭窗口（既有逻辑保留）
- 点击区域 ≥ 32px（桌面标准）

---

**下一步**：评审通过后，将上述 Token 与坐标直接落地到 `main.cpp` 的 GDI 绘制代码。
